import { CardProviderProps } from './types';
export declare function SmartCardProvider({ client, cacheOptions, storeOptions, authFlow, children, }: CardProviderProps): JSX.Element;
export { CardProviderProps as ProviderProps };
export default SmartCardProvider;
