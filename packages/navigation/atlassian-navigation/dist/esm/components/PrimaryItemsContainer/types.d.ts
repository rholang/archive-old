import { ReactNodeArray, ReactNode } from 'react';
export declare type PrimaryItemsContainerProps = {
    moreLabel: ReactNode;
    items: ReactNodeArray;
};
export declare type PrimaryItemsContainerSkeletonProps = {
    count: number;
};
