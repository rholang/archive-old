import { ReactChild } from 'react';
interface Props {
    onClick?: () => void;
    children?: ReactChild;
    testId?: string;
}
declare const _default: (props: Props) => JSX.Element;
export default _default;
