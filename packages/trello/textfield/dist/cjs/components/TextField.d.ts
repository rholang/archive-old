import * as React from 'react';
import { TextFieldProps } from '../types';
export declare function TextField(textFieldProps: TextFieldProps & React.HTMLProps<HTMLButtonElement>): JSX.Element;
export declare namespace TextField {
    var defaultProps: {
        appearance: string;
    };
}
export default TextField;
